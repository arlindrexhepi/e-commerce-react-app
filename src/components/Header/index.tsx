import StoreUpdates from "./StoreUpdates";

const Header = () => {
  return (
    <header className="bg-secondary">
      <StoreUpdates />
      <p>Header!</p>
    </header>
  );
};

export default Header;
